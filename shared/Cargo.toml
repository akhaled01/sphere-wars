[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
glam = { version = "0.25", features = ["serde"] }
bevy_reflect = "0.16"
rand = "0.8"
